# TODO — что ещё не сделано

Рабочий чек-лист, не история (в отличие от [CHANGELOG_ru.md](CHANGELOG_ru.md) — тот копится в
конец, этот файл наоборот: вычёркивай/удаляй пункт, когда он сделан, чтобы список
оставался актуальным и коротким). Список тем по урокам — отдельно, в
[CURRICULUM_ru.md](CURRICULUM_ru.md).

## Контент

- [ ] **SFI kurs D** — только 3 урока, **kurs C** — 5, **kurs B** — 9. В
  `CURRICULUM_ru.md` под каждым уровнем расписано по 25 тем — последний оставшийся
  пробел в плане контента по уровням теперь, когда весь SVA grund (delkurs 1-4) и весь
  SFI kurs A готовы. Хороший кандидат на заход параллельными агентами (подход описан в
  CHANGELOG за 2026-09-05 и 2026-09-07).
- [ ] Грамматические списки `CURRICULUM_ru.md`: **у 110 из 200 пунктов уже есть
  отдельный урок** — пункты 1-20 SVA grund delkurs 1-3 плюс все 25 пунктов delkurs 4
  (двуязычная RU/EN теория через `theory_ru.md`) и все 25 пунктов SFI kurs A (у 19 —
  свой урок, добавленный в список тем после пункта 25 так же, как это делает SVA
  grund; 6 отмечены сделанными по ссылке на тему, которая уже покрывала пункт, без
  дублирования). Остальные 90 пунктов (75 у SFI kurs B–D и пункты 21-25 у SVA grund
  delkurs 1-3) по-прежнему разбросаны по теории тематических уроков, а не вынесены в
  отдельные. Ещё есть три справочные статьи в
  `content/grammar/`.
- [ ] «Настоящий формат текстового урока» (поле для текста для чтения, типы вопросов на
  понимание прочитанного, UI `LessonPage` — см. `SPEC.md` §5) так и не спроектирован.
  Каждая тема SVA grund, которой он в идеале нужен (чтение рассказа и пересказ, referat
  со ссылкой на источник, анализ художественного текста, källkritik), вместо этого
  вышла как обычный лексический/фразовый урок, обучающий лексике навыка, а не
  встраивающий настоящий текст. Это осознанное, повторяющееся упрощение по всем 100
  темам SVA grund, а не пробел на каком-то одном уровне — стоит пересмотреть как
  настоящую фичу, если лексическая/фразовая версия окажется на практике слишком
  поверхностной.
- [ ] Ни один из 246 уроков не сгруппирован в серию (`part`/`series` из SPEC §5.2) —
  всё ещё укладывается в лимит 5 минут по одной теме. Если появится тема шире —
  делить на части, а не урезать.

## Функциональность из идей в SPEC §18 (пока не реализовано)

- [ ] Ежедневный челлендж (5 смешанных вопросов, один заход в день, щедрая награда)
- [ ] Режим карточек (флеш-карты без оценки, просто пролистать словарь урока)
- [ ] «Слово дня» на главной странице
- [ ] Мастерская предложений (сборка предложений из известных слов, упор на V2)
- [ ] Диктант на слух (TTS читает предложение → пользователь печатает)
- [ ] Тренировка произношения (Web Speech API *recognition*, распознавание речи —
  скорее всего только Chrome, деградировать мягко)
- [ ] «Музей ошибок» — страница личных повторяющихся ошибок с тренировкой в один тап
- [ ] Отдельные карточки шведской культуры (fika, lagom, jantelagen…) — сейчас это
  частично покрыто `content/history/*.json`, но там фокус на исторических фактах, а
  не на культурных нормах повседневной жизни
- [ ] Тепловая карта активности на `/stats` (в духе GitHub contributions)
- [ ] Достижения как монументы на карте города (сейчас — просто список на `/stats`,
  в SPEC §8.5 идея была показывать их прямо на `CityMap`)
- [ ] Сезонные скины по календарю (Lucia 13 декабря, midsommar, Kanelbullens dag
  4 октября, День Швеции 6 июня — с оформлением по SPEC §12.7)
- [ ] Локальный лидерборд «эта неделя против прошлой»
- [ ] Опциональная облачная синхронизация (см. SPEC §16, идея 15 — сознательно вне
  v1, ломает принцип «без бэкенда»)

## Известные несостыковки SPEC ↔ код

- [ ] **Тип вопроса `multi`** описан в SPEC.md/SPEC_ru.md (§5.6 таблица типов и §14
  фаза 6), но не реализован: в `src/content/schema.ts` `questionSchema` — только `mc`,
  `type-answer`, `gap`, `order`, `match`, `listen`, `true-false`. Либо реализовать
  (множественный выбор с частичным зачётом), либо вычеркнуть из SPEC — сейчас документ
  обещает то, чего нет в коде.
- [ ] Правило «эра открывается по XP **и** по числу построек предыдущей эры» (SPEC §8.2)
  в коде не реализовано — `useCurrentEra()` смотрит только на порог XP
  (`src/store/city.ts`). Сознательное упрощение, упомянутое в SPEC §8.2 примечанием, но
  стоит либо доделать, либо снять формулировку «и» из SPEC.

## Полировка / техдолг

- [ ] Нормальные PNG-иконки 192×192 и 512×512 для `manifest.webmanifest` — сейчас
  переиспользуется `favicon.svg`, из-за чего PWA-инсталляция не проходит некоторые
  строгие проверки (например, на iOS).
- [ ] Иллюстрации зданий — сейчас плоские переиспользуемые SVG-иконки по категориям
  (`src/components/city/icons.tsx`, 15 штук на 22 здания), а не бесподобные
  иллюстрации на каждое здание, как задумано в SPEC §11.5. Достаточно для MVP, но
  разные здания одной эры визуально похожи.
- [ ] Главный JS-чанк всё ещё 254 КБ (после code-splitting по страницам), а
  `TheoryView` (рендерер markdown) — ещё 159 КБ; оба можно дальше делить, если станет
  ощутимо на слабых сетях. (Чанк `registry`, который раньше весил 2.3 МБ и ломал шаг
  precache PWA, теперь делится по одному на курс через `manualChunks` в
  `vite.config.ts` — см. запись CHANGELOG за 2026-09-06.)
- [ ] Нет юнит-тестов на `src/components/**` (React Testing Library установлен, но
  не используется) — вся текущая проверка UI идёт через ручные e2e-прогоны Playwright
  в разговоре, они нигде не сохранены как воспроизводимый тест.
- [ ] `scripts/validate-content.ts` не проверяет согласованность форм слов
  (`nounForms`/`verbForms`) — можно ошибиться в спряжении, и никто не заметит, кроме
  внимательного читателя. Целенаправленной проверки правильности шведской грамматики
  нет и не планируется (это открытый риск, см. `CURRICULUM_ru.md`: агенты писали формы
  только когда были уверены, но 100%-й гарантии нет).
- [ ] `package.json` содержит два способа деплоя — GitHub Actions workflow (основной,
  автоматический) и `npm run deploy` через `gh-pages` (ручной, добавлен в какой-то
  момент параллельно). Стоит определиться и оставить один, чтобы не путаться.

## Не начато вообще

- [ ] Полноценная страница `/grammar` — сейчас 3 статьи (порядок слов, артикли,
  группы глаголов); хорошие кандидаты на добавление: прилагательные и согласование,
  предлоги места и времени, придаточные предложения (когда появится B1).
- [ ] i18n на шведском как языке интерфейса (сознательно отложено в SPEC §0 — не
  путать с шведским как изучаемым языком, это уже полностью работает).
